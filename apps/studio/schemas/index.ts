import { issueSchema } from "./issue";
import { articleSchema } from "./article";
import { settingsSchema } from "./settings";

export const schemaTypes = [issueSchema, articleSchema, settingsSchema];
